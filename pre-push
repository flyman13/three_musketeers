#!/bin/bash

echo "🚀 Перевірка тестів перед пушем..."

# Запускаємо RSpec
bundle exec rspec

# Зберігаємо результат тестування
status=$?

if [ $status -ne 0 ]; then
  echo "❌ ТЕСТИ НЕ ПРОЙШЛИ! Пуш заблоковано. Виправ помилки, інакше нічого не вийде."
  exit 1
fi

echo "✅ Тести пройшли успішно. Пушимо..."
exit 0

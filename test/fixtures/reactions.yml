one:
  account: one
  post: one
  target_type: "Post"
  target_id: <%= ActiveRecord::FixtureSet.identify(:one, :posts) %>

two:
  account: two
  post: one
  target_type: "Post"
  target_id: <%= ActiveRecord::FixtureSet.identify(:one, :posts) %>
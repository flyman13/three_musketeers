<div class="posts-index__container">
  <header class="posts-index__header">
    <h2>Three Musketeers insts</h2>
    <div class="posts-index__header-actions">
      <% if current_account %>
        <span>Привіт, <b><%= current_account.username %></b>!</span>
        <%= link_to "Вийти", logout_path, data: { turbo_method: :delete }, class: "link-danger" %>
      <% else %>
        <%= link_to "Увійти", login_path, class: "" %>
        <%= link_to "Реєстрація", new_account_path, class: "link-strong" %>
      <% end %>
    </div>
  </header>

  <div class="new-post-wrap">
    <%= link_to "+ Новий допис", new_post_path, class: "new-post-btn" %>
  </div>

  <% @posts.each do |post| %>
    <div class="post-card">
      
      <div class="post-meta-row">
        <strong><%= post.account&.username || "Анонім" %></strong>
        <% if post.account == current_account %>
          <%= link_to "Видалити", delete_post_path(post), 
                      data: { confirm: "Ви впевнені?" }, 
                      class: "link-danger comment-action-link", title: "Видалити" %>
        <% end %>
      </div>

      <% if post.image.attached? %>
        <div class="post-image">
          <%= image_tag post.image, class: "" %>
        </div>
      <% end %>
      <p class="post-body"><%= post.body %></p>
      
      <hr>

      <div>
        <%= button_to "❤️ #{post.reactions.count}", like_post_path(post), method: :post, class: "reactions-btn" %>
      </div>

      <div class="comments-wrap">
        <% post.comments.each do |comment| %>
          <div class="comment-item">
            <p class="comment-author">
              <b><%= comment.account&.username || "Анонім" %>:</b> <%= comment.body %>
            </p>
            
            <div class="comment-actions">
              <%= button_to "👍 #{comment.comment_reactions.count}", 
                            like_post_comment_path(post, comment), 
                            method: :post, 
                            class: "", title: "Like comment" %>

              <% if comment.account == current_account %>
                <%= link_to "🗑️", delete_post_comment_path(post, comment), 
                            data: { confirm: "Видалити коментар?" }, 
                            class: "comment-action-link" %>
              <% end %>
            </div>
          </div>
        <% end %>

        <% if current_account %>
          <div class="comment-form-wrap">
            <%= render 'comments/form', post: post %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>